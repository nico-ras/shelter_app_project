<table id="shelters">
  <thead>
    <tr>
      <th>Name</th>
      <th>Total rescatados</th>
      <th>Adoptados</th>
      <th>En refugio</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <!--<% @shelters.each do |shelter| %>
      <tr>
        <td><%= shelter.name %></td>
        <td><%= shelter.animals.count %></td>
        <td><%= shelter.animals.adopted.count %></td>
        <td><%= shelter.animals.not_adopted.count %></td>
        <td><%= link_to 'Show', shelter %></td>
        <td><%= link_to 'Edit', edit_shelter_path(shelter) %></td>
        <td><%= link_to 'Destroy', shelter, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        <td><%=  link_to 'Animals' , shelter_animals_path(shelter) %></td>

        <p>
  

      </tr>
    <% end %>-->
  </tbody>
</table>

<br>

<% if current_user.editor? %>

<%= link_to 'New Shelter', new_shelter_path %>

<% end %>